# Service Health Dashboard (Node.js + TypeScript + React)

Small fullstack monitoring project:

- Node/Express backend exposes simple health and service endpoints
- React + Vite frontend shows a live status dashboard
- Easy to extend with real HTTP checks and device metrics

Run:

- .\scripts\run.ps1
  Then open: http://localhost:5174
