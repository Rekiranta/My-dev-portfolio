# Realtime Board (Node.js + TypeScript + WebSocket)

Small fullstack project (backend + frontend) without Python:
- Express serves a tiny frontend
- WebSocket backend broadcasts updates to all connected browsers
- State persisted to `data/state.json`

Run:
- .\scripts\run.ps1
Then open: http://localhost:8090
