# Study Tracker (Flask + SQLite)

A small web app for tracking courses, credits and grades.

## Tech stack

- Python
- Flask
- SQLite
- HTML templates

## Setup

Create and activate a virtual environment (optional but recommended), then:

```bash
pip install flask
python app.py
```
