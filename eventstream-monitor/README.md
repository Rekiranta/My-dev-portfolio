#Edge Device Data Pipeline (Node.js + TypeScript + EventStream)

Small fullstack project (backend + frontend) without Python:

Express server simulates industrial edge devices

Deterministic sensor readings + calibration + fault injection

Browser UI streams live data using Server-Sent Events (SSE)

State kept in memory (no database)

Run:

.\scripts\run.ps1

Then open:
http://localhost:8093
